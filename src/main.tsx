import React from 'react'
import ReactDOM from 'react-dom/client'
import KeysCountdown from '../ericvacaymain'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KeysCountdown />
  </React.StrictMode>,
)
